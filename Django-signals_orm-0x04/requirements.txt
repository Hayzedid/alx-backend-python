Django>=4.2.0,<5.0.0
# Core Django framework for the messaging application

# Additional packages that might be useful for development
# (These are optional but recommended for a complete development environment)

# For testing and development
pytest-django>=4.5.0
coverage>=7.0.0

# For API development (if needed in the future)
djangorestframework>=3.14.0

# For better admin interface (optional)
django-admin-interface>=0.19.0

# For debugging (development only)
django-debug-toolbar>=4.0.0
